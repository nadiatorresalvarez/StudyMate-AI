@* 
ARCHIVO DEPRECADO - RESPALDO HISTÓRICO
Este archivo ha sido reemplazado por Pages/Auth/Login.razor con Google Sign-In
NO TIENE RUTA ACTIVA - Se mantiene solo como referencia histórica
*@

@inject NavigationManager NavManager

<!-- Esta página ha sido reemplazada por Pages/Auth/Login.razor -->
<!-- Redirigir automáticamente -->

@code {
    protected override void OnInitialized()
    {
        // Esta página antigua redirige a la nueva ubicación
        // En la práctica, ambas tienen la misma ruta @page "/login"
        // así que esta nunca se ejecutará. Se mantiene por compatibilidad.
        base.OnInitialized();
    }
}
